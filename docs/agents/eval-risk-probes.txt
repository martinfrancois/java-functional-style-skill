# Historical Eval Risk Probes

This file is intentionally empty for the new package until hosted functional-style eval history
exists.

Add entries only after scored with-context failures are observed during tuning, using:

```text
<suite>:<scenario-name>
```
