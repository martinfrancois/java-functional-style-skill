{
  "context": "Reference implementation: identity value mappers and merge callbacks in toMap indexes.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Creates the requested artifact",
      "max_score": 8,
      "description": "Creates LoanIndex.java with both methods, the nested Loan record, needed imports, and Java 17-compatible code."
    },
    {
      "name": "Preserves index semantics",
      "max_score": 7,
      "description": "byLoanId keeps the first loan per id and encounter order (for example LinkedHashMap); latestByMemberId keeps the loan with the latest dueDate per member, keeps the first on ties, and preserves member encounter order."
    },
    {
      "name": "Uses Function.identity for the value mapper",
      "max_score": 50,
      "description": "Both indexes use Function.identity() (with the java.util.function.Function import) as the value mapper rather than a hand-written identity lambda such as loan -> loan or a custom identity helper."
    },
    {
      "name": "Keeps merge callbacks readable",
      "max_score": 30,
      "description": "The latest-due-date merge is expressed as a one-expression callback that reads clearly (for example BinaryOperator.maxBy over Comparator.comparing(Loan::dueDate) with tie handling, or a named helper method), not a block lambda with local variables or branching inside the collector call."
    },
    {
      "name": "Stays focused",
      "max_score": 5,
      "description": "No unrelated abstractions, caching, concurrency, extra public API, or dependency changes."
    }
  ]
}
