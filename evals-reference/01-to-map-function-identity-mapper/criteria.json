{
  "context": "Reference focused implementation: identity mapper callbacks should use JDK identity functions only when they are truly identity functions, while collector semantics remain unchanged.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Creates coherent Java 17 artifact",
      "category": "safety",
      "max_score": 10,
      "description": "Creates IdentityMapperCleanup.java with the requested methods, nested records, imports, and Java 17-compatible code."
    },
    {
      "name": "Preserves collector behavior",
      "category": "safety",
      "max_score": 20,
      "description": "Preserves the original key mappers, merge functions, explicit HashMap and LinkedHashMap suppliers, returned map types, encounter-order implications, and snapshot conversion behavior."
    },
    {
      "name": "Uses JDK identity functions for identity mappers",
      "category": "functional_style",
      "max_score": 35,
      "description": "Replaces the true identity key and value mapper lambdas with Function.identity() and adds the needed import instead of keeping x -> x style callbacks or custom identity helpers."
    },
    {
      "name": "Does not replace non-identity transformations",
      "category": "functional_style",
      "max_score": 20,
      "description": "Keeps the snapshot value mapper as a real transformation and does not replace it with Function.identity(), remove it, or otherwise change CardSnapshot creation."
    },
    {
      "name": "Keeps cleanup focused",
      "category": "maintainability",
      "max_score": 15,
      "description": "Avoids unrelated stream rewrites, API redesign, new dependencies, caching, concurrency, or broad style churn."
    }
  ],
  "metadata": {
    "invocation": "natural",
    "task_type": "cleanup",
    "evidence_type": "focused_reference",
    "reference_selection": "Focused reference coverage for identity function mapper cleanup.",
    "runtime_reference_overlap_rationale": "Allowed only because this is reference-suite focused coverage, not ordinary broad lift."
  }
}
